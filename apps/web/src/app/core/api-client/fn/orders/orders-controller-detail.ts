/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderDetailResponseDtoOutput } from '../../models/order-detail-response-dto-output';

export interface OrdersControllerDetail$Params {
  id: string;
}

export function ordersControllerDetail(http: HttpClient, rootUrl: string, params: OrdersControllerDetail$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderDetailResponseDtoOutput>> {
  const rb = new RequestBuilder(rootUrl, ordersControllerDetail.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderDetailResponseDtoOutput>;
    })
  );
}

ordersControllerDetail.PATH = '/api/orders/{id}';
