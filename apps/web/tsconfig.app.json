{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "../../dist/out-tsc",
    // See apps/api/tsconfig.app.json for why this is needed once src
    // imports libs/shared/* via the @lavenet/* path alias.
    "rootDir": "../..",
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts", "src/**/*.test.ts"]
}
